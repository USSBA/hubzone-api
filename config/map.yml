default: &defaults
  google_api_key: <%= ENV.fetch('HUBZONE_GOOGLE_API_KEY') { 'SetYourGoogleAPIKeyInYour.envFile' } %>

production:
  <<: *defaults

staging:
  <<: *defaults

training:
  <<: *defaults

qa:
  <<: *defaults

demo:
  <<: *defaults

dev:
  <<: *defaults

development:
  <<: *defaults

developmentdocker:
  <<: *defaults

test:
  <<: *defaults
